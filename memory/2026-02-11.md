# 2026-02-11

## Llora / fine-tuning source of truth

Repo: https://github.com/milwrite/quimbot (checked out at `/home/milwrite/.openclaw/workspace`).

Key locations:
- `fine-tuning/WORKFLOW.md` — workflow doc
- `fine-tuning/data/` — training datasets (e.g. `stage1_train.jsonl`, `ultrachat_200k_train_sft*.jsonl`)
- `fine-tuning/*.log` — training run logs (e.g. `production_run_20260205_183156.log`)
- `fine-tuning/checkpoints/` — run configs + `training_log.json`

Notes:
- Prior training log shows completion with checkpoints saved (see `fine-tuning/production_run_20260205_183156.log`).
